{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/camera/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/camera",
        "main": "libs/camera/src/index.ts",
        "tsConfig": "libs/camera/tsconfig.lib.json",
        "assets": ["libs/camera/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/camera/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/camera"],
      "options": {
        "jestConfig": "libs/camera/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
